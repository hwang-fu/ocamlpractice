(** Animated Tower of Hanoi in a Graphics window. Disc count, timing, and
    scene geometry are private to the implementation. *)

(** [run ()] opens the window, animates the full solution, and returns once
    a key is pressed or the window is closed. *)
val run : unit -> unit
